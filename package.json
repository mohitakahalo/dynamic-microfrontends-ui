{
  "name": "dynamic-microfrontends-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:microfrontends && npm run build:shell",
    "build:microfrontends": "npm run build:cart && npm run build:checkout",
    "build:shell": "cd shell-app && npm install && npm run build",
    "build:cart": "cd cart-app && npm install && npm run build",
    "build:checkout": "cd checkout-app && npm install && npm run build"
  }
} 
